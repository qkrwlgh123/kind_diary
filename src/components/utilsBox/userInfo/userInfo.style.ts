import styled from "styled-components";

const UserInfoBox = styled.div`
  font-size: 15px;
  display: flex;
  font-weight: 700;
`;

const IconBox = styled.div`
  color: red;
  margin-left: 3px;
`;

export default { UserInfoBox, IconBox };
