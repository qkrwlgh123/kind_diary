import styled from "styled-components";

const ButtonContainer = styled.div`
  display: flex;
  align-items: start;
  justify-content: space-between;

  margin-bottom: 20px;
`;

const SelectedDateBox = styled.div`
  font-size: 18px;
`;

export default { ButtonContainer, SelectedDateBox };
